{
  "name": "zoolander",
  "description": "Derek Zoolander's Corporate Style Guide to Help Product Teams Build Ridiculously Good Looking Websites",
  "version": "0.13.46",
  "authors": [
    "Rackspace Web Team <rswebdev@rackspace.com>"
  ],
  "scripts": {
    "font:open": "node_modules/fontello-cli/bin/fontello-cli open --config iconfont/config.json",
    "font:update": "node_modules/fontello-cli/bin/fontello-cli install --config iconfont/config.json --css iconfont/css --font iconfont/font",
    "font:build": "./scripts/build-fonts.sh",
    "harp": "harp ",
    "harp:compile": "npm run harp compile styleguide dist",
    "start": "harp server styleguide",
    "gh-pages": "./scripts/build-pages.sh",
    "preversion": "npm run harp compile styleguide dist && git add -f dist",
    "postversion": "git push && git push --tags",
    "sass:compile": "node-sass --output-style=compressed --quiet -o /tmp styleguide/css",
    "sass-lint": "sass-lint -v",
    "hooks": "./scripts/hooks.sh",
    "postinstall": "npm run hooks"
  },
  "repository": "https://github.com/rackerlabs/zoolander",
  "license": "MIT",
  "dependencies": {
    "bootstrap-sass": "^3.3.6",
    "fontello-cli": "^0.4.0",
    "harp": "^0.20.1"
  },
  "devDependencies": {
    "fs-extra": "^0.30.0",
    "node-sass": "^3.8.0",
    "sass-lint": "^1.0.1",
    "request": "^2.72.0",
    "shortid": "^2.2.6",
    "walk": "^2.3.9"
  },
  "files": [
    "dist",
    "scripts",
    "styleguide",
    "README.md"
  ]
}
